 var b=["zaid", "bin", "farooq"];
var a=prompt("Enter your name");
if (a=="zaid" || a=="bin" || a=="farooq") //this logic can also be used
if (b.includes(a)) //function used to identify element in the array.
{
alert("Welcome Zaid");
    }
else
{
alert("get out");   
    }
